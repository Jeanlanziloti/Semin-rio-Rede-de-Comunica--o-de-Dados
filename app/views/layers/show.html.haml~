- model_class = Layer
.page-header
  %h1= @layer.name

= simple_format @layer.description

.form-actions
  = link_to "Voltar", layers_path, :class => 'btn'
  = link_to "Editar", edit_layer_path(@layer), :class => 'btn'
  = link_to "Excluir", layer_path(@layer), :method => "delete", :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) }, :class => 'btn btn-danger'
